import {SmallInputNumberDirective} from './small-input-number.directive';

describe('AutoWidthDirective', () => {
  it('should create an instance', () => {
    // const directive = new SmallInputNumberDirective();
    // expect(directive).toBeTruthy();
  });
});
